# Intel Release

Place the Intel build here for release upload.

Recommended filename:

- `PDF-Toolbox-macOS-Intel-1.0.0.zip`

Build this on:

- an Intel Mac, or
- a true x86_64 Python / Rosetta build environment

For older macOS targets, you may need an older compatible Python and dependency set.
